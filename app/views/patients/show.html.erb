<h1>Patient's Show Page</h1>
<h3><%= @patient.name %></h3>

<ul><% @patient.appointments.each do |app| %>
    <!-- <li> Appointment Time: < app.appointment_datetime.strftime('%A'+ ', ' + '%C') %> -->
        <li> Appointment Time: <%= app.appointment_datetime.strftime('%B %d, %G at %R') %>
         Doctor: <%= link_to app.doctor.name, doctor_path(app.doctor) %></li>
    <br />
    <% end %>
</ul>
